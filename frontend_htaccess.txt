Options -MultiViews
RewriteEngine On

# SPA routing — todo lo que no sea archivo o carpeta real va a index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
